'use strict';
const Extension = require('./Extension');
const { INPUT_METHOD, PLATFORMS, ICONS } = require('./constants');
const { log } = require('./utils');

module.exports = {
	Extension,
	log,
	INPUT_METHOD,
	PLATFORMS,
	ICONS
};
